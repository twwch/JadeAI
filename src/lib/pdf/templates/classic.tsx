// @react-pdf/renderer Classic template
// This would define PDF-specific components using @react-pdf/renderer primitives
// (Document, Page, View, Text, etc.)
// For the initial implementation, we use the HTML preview templates
// which can be printed to PDF via the browser

export {};
